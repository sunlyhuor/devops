apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx-app
spec:
  selector:
    app: nginx-label
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: ClusterIP #NodePort|LoadBalancer|ExternalName
